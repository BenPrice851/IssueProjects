<?xml version="1.0" encoding="UTF-8"?>
<ContentView xmlns="http://xamarin.com/schemas/2014/forms" 
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:d="http://xamarin.com/schemas/2014/forms/design"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             mc:Ignorable="d"
             x:Class="IssuesApp.CustomView"
             x:Name="this">
  <ContentView.Content>
      <StackLayout>
          <Label Text="Custom Control" />
            <!--Text="{Binding Source={x:Reference this}, Path=ClassBinding.Result}"-->
            <Label Text="{Binding Result}"/>
            <!--Command="{Binding Source={x:Reference this}, Path=ClassBinding.TakePhotoCommand}"-->
            <Button Text="Take photo" Command="{Binding TakePhotoCommand}"/>
        </StackLayout>
  </ContentView.Content>
</ContentView>